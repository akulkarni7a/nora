const { NlpManager } = require('./lib');

async function main() {
  const manager = new NlpManager({ languages: ['he', 'en'] });
  let answer = await manager.process('מנהל המערכת יתקשר אליך. תודה.');
  console.log(answer);
  answer = await manager.process('1');
  console.log(answer);

}

main();
